// nothing on the page for now — just the fluid. content comes back later, the
// section components are still sitting in components/sections ready to wire up.
export default function Home() {
  return null;
}
